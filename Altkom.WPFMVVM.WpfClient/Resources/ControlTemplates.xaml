<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Altkom.WPFMVVM.WpfClient.Resources">

    <ControlTemplate x:Key="MyButtonTemplate" TargetType="Button">

        <Grid>
            
            <Ellipse Fill="{TemplateBinding Background}"
                     StrokeThickness="3"
                     Stroke="{TemplateBinding BorderBrush}"                     
                     />
            
            <ContentPresenter 
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                />

        </Grid>

    </ControlTemplate>

    <ControlTemplate x:Key="RounderButtonTemplate" TargetType="Button">
        <Grid>
            <Border BorderBrush="Black" Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="5">
                
                <ContentPresenter  HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
            
            </Border>
        </Grid>
    </ControlTemplate>

    <!--<Style TargetType="Button">
        <Setter Property="Template" Value="{StaticResource RounderButtonTemplate}" />
    </Style>-->
    
</ResourceDictionary>